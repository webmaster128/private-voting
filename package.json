{
  "name": "private-voting-monorepo-root",
  "license": "Apache-2.0",
  "author": {
    "name": "Simon Warta",
    "url": "https://github.com/webmaster128"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "lerna run format",
    "format-text": "prettier --write --prose-wrap always --print-width 80 \"./*.{md,txt}\" && lerna run format-text",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^12.0.0",
    "@typescript-eslint/eslint-plugin": "^2",
    "@typescript-eslint/parser": "^2",
    "eslint": "^6",
    "eslint-config-prettier": "^6",
    "eslint-plugin-prettier": "^3.0.1",
    "eslint-plugin-simple-import-sort": "^5",
    "jest": "^24.7.1",
    "lerna": "^3.15.0",
    "prettier": "^1.18.2",
    "typescript": "~3.7"
  }
}
